# GitPowerWorktree

このリポジトリは、Gitのワークツリーをコマンドを利用して動的に管理できるようにすることを目的として作成されました。

# 設計
- `git-pt`がコマンドプレフィックスです
- `create`/`clone`/`tree`/`openin`/`config`/`help`をサポートします
- これらのコマンドはコンピューターの`git`を利用します
